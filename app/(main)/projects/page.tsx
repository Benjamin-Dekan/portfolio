import ProjectsBackground from "@/app/components/ui/ProjectsBackground";

const ProjectsPage = () => {
  return (
    <div className="relative grow flex flex-col w-full h-full">
      <ProjectsBackground />
    </div>
  );
};

export default ProjectsPage;
